import { ClickoutsiteDirective } from './clickoutsite.directive';

describe('ClickoutsiteDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickoutsiteDirective();
    expect(directive).toBeTruthy();
  });
});
